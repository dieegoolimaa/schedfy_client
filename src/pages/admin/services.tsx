import { Layout } from "@/components/Layout";

export default function AdminServicesPage() {
  return (
    <Layout>
      <div className="p-6">
        <h1 className="text-2xl font-bold">Serviços (Admin)</h1>
        <p className="text-sm text-[var(--color-muted-foreground)] mt-2">
          Página para gerenciar serviços do negócio. (placeholder)
        </p>
      </div>
    </Layout>
  );
}
